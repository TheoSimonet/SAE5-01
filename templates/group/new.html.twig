{% extends 'base.html.twig' %}

{% block title %}Create Group{% endblock %}

{% block body %}
    <h1>Create Group</h1>

    {{ form_start(form) }}
    {{ form_row(form.lib) }}
    {{ form_row(form.type) }}
    <button type="submit" class="btn btn-primary">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('group_index') }}" class="btn btn-secondary">Back to Groups</a>
{% endblock %}
