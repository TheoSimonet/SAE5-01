{% extends 'base.html.twig' %}

{% block title %}Edit Group{% endblock %}

{% block body %}
    <h1>Edit Group</h1>

    {{ form_start(form) }}
    {{ form_row(form.lib) }}
    {{ form_row(form.type) }}
    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('group_show', {'id': group.id}) }}" class="btn btn-secondary">Back to Group</a>
{% endblock %}
