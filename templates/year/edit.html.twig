{% extends 'base.html.twig' %}

{% block title %}Edit Year{% endblock %}

{% block body %}
    <h1>Edit Year</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Save Changes</button>
    {{ form_end(form) }}
{% endblock %}
